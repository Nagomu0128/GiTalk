steps:
  # Step 0: Lint
  - name: "node:20"
    entrypoint: "bash"
    args:
      - "-c"
      - |
        npm install -g pnpm && \
        pnpm install --frozen-lockfile && \
        pnpm lint
    dir: "backend"

  # Step 1: DB Migration (via Cloud SQL Auth Proxy)
  - name: "node:20"
    entrypoint: "bash"
    args:
      - "-c"
      - |
        npm install -g pnpm && \
        pnpm install --frozen-lockfile && \
        echo "Running database migration..." && \
        DATABASE_URL="$$DATABASE_URL" pnpm db:push
    dir: "backend"
    secretEnv: ["DATABASE_URL"]

  # Step 2: Docker build
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "${_ARTIFACT_REGISTRY_URL}/${_IMAGE_NAME}:${COMMIT_SHA}"
      - "-t"
      - "${_ARTIFACT_REGISTRY_URL}/${_IMAGE_NAME}:latest"
      - "-f"
      - "backend/Dockerfile"
      - "backend"

  # Step 3: Docker push
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "--all-tags"
      - "${_ARTIFACT_REGISTRY_URL}/${_IMAGE_NAME}"

  # Step 4: Deploy to Cloud Run
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: "gcloud"
    args:
      - "run"
      - "deploy"
      - "${_CLOUD_RUN_SERVICE}"
      - "--image"
      - "${_ARTIFACT_REGISTRY_URL}/${_IMAGE_NAME}:${COMMIT_SHA}"
      - "--region"
      - "${_REGION}"
      - "--add-cloudsql-instances"
      - "${_CLOUDSQL_INSTANCE}"

availableSecrets:
  secretManager:
    - versionName: "projects/${PROJECT_ID}/secrets/gitalk-database-url/versions/latest"
      env: "DATABASE_URL"

options:
  default_logs_bucket_behavior: REGIONAL_USER_OWNED_BUCKET

substitutions:
  _ARTIFACT_REGISTRY_URL: ""
  _IMAGE_NAME: ""
  _CLOUD_RUN_SERVICE: ""
  _REGION: ""
  _CLOUDSQL_INSTANCE: ""
